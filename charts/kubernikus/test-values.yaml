domain: xyz.com
openstack:
  auth_url: http://xyz.com
  auth_user_id: xyz
  auth_user_password: xyz
  auth_domain: xyz
  auth_project: xyz
  auth_project_domain: xyz
  region: xy-xy-1
dex:
  clientSecret: uhu-ah-secret
  ldap:
    config:
      host: ldap.host.xyz
      bindDN: urks=burks
      bindPW: ldap-pass
      userSearch:
        baseDN: user=looser
        filter: hase=nase
      groupSearch:
        baseDN: group=pup
        filter: igel=fuchs
  connectors:
    - name: ldap
      id: ldap1
      projectID: project1
    - name: ldap2
      id: ldap2
      projectID: project2
